# Standards: 0.11
---

# 1.6.1.3 Ensure SELinux policy is configured (Scored)

- name: 1.6.1.3 Ensure SELinux policy is configured (Scored)
  selinux:
    policy: targeted
    state: "{{ vt_control_selinux_state }}"
  tags:
      - level-1
      - section-1
      - "1.1.11"
      - scored
